var x = 2

switch (x) {
    case 0: console.log("x is 0"); break;
    case 1: console.log("x is 1"); break;
    case 2: console.log("x is 2"); break;
    default: console.log("x is not one of 0, 1 or 2"); break;
}
